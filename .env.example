# Get this from: https://discord.com/developers/applications
# Navigate to your application → Bot → Token → Reset Token/Copy
DISCORD_TOKEN=abc123
GITHUB_TOKEN=abc123

# Optional: Specific guild (server) ID to operate in
# Right-click server icon → Copy Server ID (requires Developer Mode)
DISCORD_SERVER_ID=123456

# Path to config.yaml (use config.yaml for Docker, or ./config.yaml for local)
CONFIG_PATH=config.yaml

# Path to FAQ YAML file
FAQ_PATH=faq.yaml

# Environment (dev or prod) - determines which .env file to load
ENV=dev

# Health check port
HEALTHCHECK_PORT=8080
